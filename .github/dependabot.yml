version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
# Conda scientific dependencies and setuptools must be updated together by hand;
# numerical baselines require explicit review. Do not add a separate pip updater.
